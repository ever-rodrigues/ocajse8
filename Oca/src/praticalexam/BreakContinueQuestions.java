package praticalexam;

public class BreakContinueQuestions {

    //Break break the code and go out the blockCode

    //Continue breaks the statement code and continues down

    //KEY CONTINUE CAN ONLY BE USED IN (LOOPS) -> FOR AND DO WHILE, WHILE LOOPS!!!!

    //IF USED IN IF -> COMPILE ERROR!!!!!!!!!!!!

    //BREAK CANNOT BE OUTSIDE LOOP!!!

    //Keyword "continue" can only be used to continue the iteration of any loop (for/while/do while)
    //otherwise it will give a compiler error.

    //keyword "break" can only be used to break out of a current loop or switch block or labelled block.

    //In order to break out of a label block, it is necessary to have the break label inside the said block label i.e.

}
