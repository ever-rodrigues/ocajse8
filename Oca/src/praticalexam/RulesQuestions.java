package praticalexam;

public class RulesQuestions {
    //PACKAGE declaration must be the first line of code otherwise compiler error

    //SPECIFIED packages must be placed in the same directory as the package name.

    //'this' reference is not available within static method otherwise it will cause compiler error.

    //this(..) and super(..) cannot be called together in class

    //this(..) or super(..) can only be called in first line of constructor.

    //FINAL METHODS CANNOT BE OVERIDDEN!!!
    //BE CAREFULL< IF YOU HAVE A SUBCLASS TRYING TO OVERIDE PUBLIC METHOD FROM SUPERCLASS ->COMPILE ERROR!!!



}
