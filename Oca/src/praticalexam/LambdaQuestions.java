package praticalexam;

public class LambdaQuestions {
    //Predicate lambdas take exactly one parameter.

    //Syntax Rules for Predicate Lambda
    //The predicate parameter can be just a variable name or it
    // can be the type followed by the variable name, all in parethesis.

    //Lambda Accept Return
    //According to the rules of lambda expressions, the keyword 'return' must be wrapped in brackets '{}'.

    //According to the rules of lambda expressions, the lambda single parameter can be cast using parenthesis.

    //LAMBDA PRECISA DE PARENTESES!!! ()-> {;}
}
