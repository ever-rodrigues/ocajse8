package praticalexam;

public class ThreadQuestions {

    //The keyword SYNCRONIZED indicates that a method can be acessed by only one thread at the time!

    //Java is multithreaded, so if the program has multiple threads
    // uncaught exceptions will terminate only in the thread where the exception occurred

//    The java.lang.StringBuilder is not thread safe.
//    That means StringBuilder methods are not synchronized

}
